{
  "_comment": [
    "Example crustify/targets/<target>/scope-config.json (user-authored,",
    "TARGET-tier). One per crustify invocation target. It names the campaign's",
    "objective and its two file sets, plus a target-local refinement of what is",
    "out of scope. Values below illustrate a run scoped to ssl/statem inside an",
    "openssl checkout; substitute for other projects."
  ],
  "_comment_layout": [
    "All crustify artifacts live under a single `crustify/` directory at the",
    "repository root (which also marks the root). REPO-ROOT-tier artifacts are",
    "project-wide: build.json + crates.json (user-authored),",
    "the stem-grouped analysis tree",
    "(analysis/<dir>/<stem>/{syms,types}.json), the CodeQL database",
    "(codeql/{db,t1,t2}/), and the shared Rust crates (rust/<crate>/).",
    "TARGET-tier artifacts live under crustify/targets/<target>/ and are",
    "invocation-scoped: this scope-config.json, the derived scope.json, and",
    "per-run agent logs (logs/<session>/).",
    "",
    "The repo root and the target id are CLI positionals",
    "(`crustify-cli <repo_root> <target> ...`) and this file's own location",
    "under `crustify/targets/<target>/` records the target. Neither is",
    "restated here: a second copy can only drift from the invocation."
  ],
  "_comment_campaign_objective": [
    "What this campaign is FOR - `port` or `wrap`. REQUIRED, with no default.",
    "",
    "IT DOES NOT AFFECT SCOPE MEMBERSHIP. `targeted`, `imported` and the `api`",
    "view are composed identically under both objectives - what a campaign",
    "COVERS is a property of the file sets below, not of what we intend to do",
    "with them. What the objective decides is how deeply the DEPENDENCY GRAPH",
    "reads the library:",
    "",
    "  `port`  walk every targeted symbol's body, so its callees and its",
    "          field-derived type uses are real ordering dependencies; a struct",
    "          defined anywhere in the targeted set keeps its full field",
    "          layout, because that layout is being reimplemented.",
    "",
    "  `wrap`  walk NO bodies. Every symbol contributes its signature only -",
    "          exactly how an imported symbol behaves under `port` - and only a",
    "          struct DEFINED in `api_headers` keeps its fields, because there",
    "          they ARE the API. Everything else orders as an opaque handle,",
    "          which is the correct shape for wrapping and is why",
    "          `query dag --full` exists for when you want to size the port",
    "          instead: it reads the same tree as if this said `port`.",
    "",
    "This is NOT the per-wave `translate --objective`, which is the verb handed",
    "to one agent over one selection (wrap | port | review | raw). This says",
    "what the whole campaign is aimed at. Narrowing to an API SURFACE is",
    "neither - that is SELECTION (`translate --file include/openssl/ssl.h`), or",
    "the `--api-only` scope flag.",
    "",
    "Ownership analysis is unaffected either way: ptr_args/ptr_ret come from the",
    "T2 tables, which cover the whole database regardless of scope."
  ],
  "campaign_objective": "port",
  "_comment_files": [
    "The campaign's two file sets. BOTH are authored whatever the objective -",
    "they describe the library, not the campaign - and both are",
    "repo-root-relative and always explicit; there is no implicit directory",
    "walk. Entries are either:",
    "  * a FILE      - `include/internal/statem.h`",
    "  * a DIRECTORY, written with a trailing slash - `ssl/` - which expands",
    "    to every source/header file beneath it",
    "`out_of_scope.paths` applies to whatever a directory entry expands to.",
    "Naming a file the build never compiled is harmless: scope.json is anchored",
    "on the T1 tables, so uncompiled candidates simply drop out.",
    "",
    "  `impl_files`   the sources - and the private headers - that IMPLEMENT",
    "                 the library. Name the implementations AND the internal",
    "                 headers that DEFINE the types: a header outside the",
    "                 target tree is never discovered on its own, and a",
    "                 header-only list would drop every function it merely",
    "                 declares.",
    "",
    "  `api_headers`  the headers that PUBLISH the API (`include/openssl/`,",
    "                 `include/libxml/`), not its sources.",
    "",
    "THE THREE DERIVED SETS. `targeted` is `impl_files` + `api_headers`,",
    "DEFINITION-anchored: an entity is targeted iff its BODY lives in a named",
    "file (or, having no body, all its declarations do). `imported` is",
    "everything targeted code REACHES that neither key names - the campaign's",
    "external dependencies, derived automatically. The `api` view is what",
    "`api_headers` DECLARES, and it CUTS ACROSS both: a public header publishes",
    "what it declares, and the bodies live in the .c files behind it. So a",
    "symbol declared in your API but defined in another library is `api` AND",
    "`imported` at once - which is exactly what a re-export is. The oracle's",
    "`--api-only` intersects with `--targeted-only` / `--imported-only` rather",
    "than excluding them, for that reason."
  ],
  "impl_files": [
    "ssl/",
    "include/internal/statem.h"
  ],
  "api_headers": [
    "include/openssl/ssl.h"
  ],
  "_comment_out_of_scope": [
    "Target-local refinement of what crustify must NOT analyze. `paths` lists",
    "repo-root-relative paths (directories with trailing `/` recurse; entries",
    "without are exact-file matches) that the composer skips while expanding",
    "`impl_files` / `api_headers`, even when a directory entry there covers",
    "them. Project-wide exclusions (e.g. `apps/`, `fuzz/`) are implicit via not",
    "being named at all - list them here only if a directory entry would",
    "otherwise pull them in. `features` lists macro feature names (typically",
    "`OPENSSL_NO_<FEATURE>` or equivalent) the user wants treated as",
    "disabled. NOTE: like `build.json`'s `features` block, this list is",
    "currently DOCUMENTATION ONLY - no composer reads it. Dead code is",
    "filtered implicitly instead: scope.json is anchored on the T1 tables, so",
    "a body elided by a disabled feature contributes no entities and its file",
    "never appears in `files`."
  ],
  "out_of_scope": {
    "paths": [],
    "features": [
      "OPENSSL_NO_DEPRECATED_3_0"
    ]
  }
}
