{
  "_comment": "Example `<repo_root>/crustify/build.json` (REPO-ROOT tier), hand-authored. It fixes the exact configure, build, and test commands used for the campaign. Increment `version` whenever any command changes; derived artifacts such as subsystems.json record that version as provenance.",
  "version": 1,
  "build_commands": {
    "configure": "./Configure no-deprecated --strict-warnings",
    "build": "make -j8",
    "test": "make test"
  }
}
